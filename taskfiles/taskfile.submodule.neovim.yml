version: '3'

vars:
  INIT: git submodule update --init
  DEINIT: git submodule deinit
  ADD: git submodule add
  LOCAL_PATH: "/neovim/.config"
  FULL_PATH: "{{.ROOT_DIR}}{{.LOCAL_PATH}}/nvim"

tasks:

  default:
    internal: true
    desc: Default
    cmds:
      - |
        echo "Working dir: {{.USER_WORKING_DIR}}"
        echo "OS: {{OS}}"
        task --list
    silent: true

  assert:
    cmds:
      - for:
          var: REQUIRED_TOOLS
          split: ','
          as: TOOL
        cmd: |
          [ -n "$(which {{ .TOOL }})" ] || ( \
            echo "ERROR: `{{ .TOOL }}` not found" && exit 1; \
          )
    silent: true
    internal: true
    vars:
      REQUIRED_TOOLS: git

  init_neovim:
    desc: Initialize neovim submodule
    cmds:
      - "{{.INIT}} {{.FULL_PATH}}"

  deinit_neovim:
    deps: [assert]
    desc: Remove neovim submodule
    prompt: ARE YOU SURE YOU WANT TO REMOVE THE SUBMODULES
    cmds:
      - "{{.DEINIT}} {{.FULL_PATH}}"
      - "rm -rf {{.ROOT_DIR}}/.git/modules{{.LOCAL_PATH}}/nvim"
      - "git rm {{.FULL_PATH}}"

  add_neovim:
    deps: [assert]
    desc: Add neovim git submodule
    prompt: ARE YOU SURE YOU WANT TO ADD THE SUBMODULE
    cmds:
      - "mkdir -p {{.ROOT_DIR}}{{.LOCAL_PATH}}"
      - "{{.ADD}} git@github.com:itzalak/nvim.git {{.FULL_PATH}}"
